# regular user for test
app_db: appdb
app_user: appuser
app_pass: "ChangeMeApp!"

# super user for vault
super_user: "vault_admin"
super_pwd: "StrongVaultAdmin!"

# Connection to each DB host; these tasks run on the DB VMs themselves.
pg_login_user: postgres            # local socket superuser
pg_login_unix_socket: /var/run/postgresql  # Debian/Ubuntu default; use None if TCP
